# Host has only a worker label — no controller label. Should fail cluster validation.
name: no-controller-cluster
version: v1

hosts:
  - name: worker-only
    uri: 10.0.0.1
    labels: [worker]
    ssh:
      user: root
